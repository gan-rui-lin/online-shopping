# Project: Online Shopping Platform

## Allowed Tools
Allow all Bash commands except those containing `rm -rf` or `rm -r` on critical paths.
Allow all file read, write, and edit operations.
Allow all glob and grep operations.
Allow all agent operations.

## Project Info
- Language: Java 17
- Framework: Spring Boot 3.3.4 + MyBatis-Plus + Spring Security + JWT + Redis
- Database: MySQL 8
- Package: com.helloworld.onlineshopping
